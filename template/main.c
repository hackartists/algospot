#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAXCOUNT 10000

typedef unsigned int input_t;

int main(){
        int count;
        input_t input;
        scanf("%d", &count);

        while(count--){
                scanf("%d",&input);
        }

        return 0;
}
